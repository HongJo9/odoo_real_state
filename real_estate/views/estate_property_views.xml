<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Vista Kanban mínima -->
    <record id="view_estate_property_kanban" model="ir.ui.view">
        <field name="name">estate.property.kanban</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <!-- Agrupación predeterminada por tipo de propiedad -->
            <kanban default_group_by="property_type_id" js_class="no_kanban_drag">

                <field name="name" />
                <field name="expected_price" />
                <field name="best_price" />
                <field name="selling_price" />
                <field name="tag_ids" />
                <field name="state" />
                <field name="property_type_id" />

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click o_kanban_card">
                            <strong>
                                <field name="name" />
                            </strong>
                            <div>
                                <t t-if="record.expected_price.raw_value"> 💰 <b>Precio Esperado:</b>
                                    <t
                                        t-esc="record.expected_price.value" />
                                </t>
                            </div>
                            <div t-if="record.best_price.raw_value"> 🏷 <b>Mejor Oferta:</b>
                                <t
                                    t-esc="record.best_price.value" />
                            </div>
                            <div t-if="record.selling_price.raw_value"> ✅ <b>Precio de Venta:</b>
                                <t
                                    t-esc="record.selling_price.value" />
                            </div>
                            <div t-if="record.tag_ids.raw_value.length"> 🏷 <b>Etiquetas:</b>
                                <t
                                    t-foreach="record.tag_ids.raw_value" t-as="tag">
                                    <span class="badge badge-primary" t-esc="tag[1]" />
                                </t>
                            </div>
                        </div>
                    </t>
                </templates>

            </kanban>
        </field>
    </record>

    <!-- Vista de lista (tree) -->
    <record id="view_estate_property_tree" model="ir.ui.view">
        <field name="name">estate.property.tree</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <tree string="Properties"
                decoration-success="state == 'offer_received' or state =='offer_accepted'"
                decoration-bf="state == 'offer_accepted'"
                decoration-muted="state == 'sold'">
                <field name="name" />
                <field name="date_availability" optional="hade" />
                <field name="bedrooms" />
                <field name="garage" />
                <field name="garden_orientation" />
                <field name="state" />
            </tree>
        </field>
    </record>


    <!-- Vista de formulario (form) -->
    <record id="estate_property_form_view" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <sheet>
                    <header>
                        <button name="action_sold" string="SOLD"
                            type="object" class="oe_highlight"
                            invisible="state in ['sold', 'canceled']"
                        />
                        <button name="action_cancel" string="CANCEL"
                            type="object" class="oe_highlight"
                            invisible="state in ['sold', 'canceled']"
                        />
                        <field name="state" widget="statusbar" />
                    </header>
                    <div class="oe_title">
                        <h1>
                            <field name="name" />
                        </h1>
                    </div>
                    <field name="tag_ids" options="{'color_field': 'color'}" widget="many2many_tags" />
                    <group>
                        <group>
                            <field name="state" />
                            <field name="garden" />
                            <field name="garden_area" invisible="not garden" />
                            <field name="garden_orientation" invisible="not garden" />
                            <field name="property_type_id"
                                options="{'no_create': True, 'no_open': True}" />
                            <field name="postcode" />
                            <field name="date_availability" />
                        </group>
                        <group>
                            <field name="best_price" />
                            <field name="expected_price" />
                            <field name="selling_price" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description" />
                                <field name="living_area" />
                                <field name="garden_area" />
                                <field name="total_area" readonly="1" />
                            </group>
                        </page>
                        <page string="Offers" name="offers">
                            <field name="offer_ids"
                                readonly="state in ('offer_accepted', 'sold', 'canceled')">
                                <tree editable="bottom" decoration-success="status == 'accepted'"
                                    decoration-danger="status == 'refused'">
                                    <field name="price" />
                                    <field name="partner_id" />
                                    <field name="validity" />
                                    <field name="date_deadline" />
                                    <button name="action_accept" type="object" icon="fa-check"
                                        invisible="status != 'pending'" />
                                    <button name="action_refuse" type="object" icon="fa-times"
                                        invisible="status != 'pending'" />
                                    <field name="status" widget="statusbar" invisible="1" />
                                </tree>
                                <form>
                                    <group>
                                        <field name="price" />
                                        <field name="partner_id" />
                                        <field name="validity" />
                                        <field name="date_deadline" />
                                    </group>
                                </form>
                            </field>
                        </page>
                        <page string="Other Information">
                            <group>
                                <field name="seller_id" />
                                <field name="buyer_id" />
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Properties">
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area" filter_domain="[('living_area', '>=', self)]" />
                <field name="facades" />
                <filter name="available" string="Disponibles"
                    domain="[('state', 'in', ['new', 'offer_received'])]" />
                <filter name="canceled" string="Cancelados"
                    domain="[('state', '=', 'canceled')]" />
                <filter string="Vendidas" name="sold" domain="[('state', '=', 'sold')]" />
                <group string="Agrupar Por">
                    <filter string="Código Postal" name="group_by_postcode"
                        context="{'group_by': 'postcode'}" />
                    <filter string="Estado" name="group_by_state"
                        context="{'group_by': 'state'}" />
                </group>

            </search>
        </field>
    </record>

    <!-- Acción que abre el modelo estate.property -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_id" ref="view_estate_property_tree" />
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{'search_default_available': 1}</field>
    </record>


</odoo>